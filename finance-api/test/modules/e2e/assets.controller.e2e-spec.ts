describe('AssetsController - 2e2 tests', () => {
  describe('GET /assets', () => {
    it('should return an array of assets', async () => {});
  });

  describe('POST /assets', () => {
    it('should create a new asset', async () => {});
    it('should return a BadRequestException if the asset already exists', async () => {});
  });
});
